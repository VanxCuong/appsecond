<% include header%>
<%
function getDateTime(a) {
  var date = new Date(a);
  var hour = date.getHours();
  hour = (hour < 10 ? "0" : "") + hour;
  var min  = date.getMinutes();
  min = (min < 10 ? "0" : "") + min;
  var sec  = date.getSeconds();
  sec = (sec < 10 ? "0" : "") + sec;
  var year = date.getFullYear();
  var month = date.getMonth() + 1;
  month = (month < 10 ? "0" : "") + month;
  var day  = date.getDate();
  day = (day < 10 ? "0" : "") + day;
  return day + "/" + month + "/" + year;
}
%>
  <div class="content-wrapper manager-users">
    <div class="container-fluid">
      <!-- Breadcrumbs-->
      <ol class="breadcrumb">
        <li class="breadcrumb-item">
          <a href="#">Dashboard</a>
        </li>
        <li class="breadcrumb-item active">Tables</li>
      </ol>
      <!-- Example DataTables Card-->
      <div class="card mb-3">
        <div class="card-header">
          <i class="fa fa-table"></i> Sửa thông tin thành viên: <%= user.user_id.email%></div>
        <div class="card-body">
          <div class="table-responsive">
            <div class="handleOrther">
                <form action="/admin/users/edit/<%= user.user_id._id%>" method="post">
                    <div class="form-group">
                      <label for="">Họ Tên</label>
                      <input type="text" class="form-control" value="<%= user.user_id.fullname%>" name="fullname" >
                      <small id="emailHelpId" class="form-text text-muted">Help text</small>
                    </div>
                    <div class="form-group">
                      <label for="">Địa Chỉ</label>
                      <input type="text" class="form-control" value="<% if(user.user_id.address){%><%= user.user_id.address%> <%}else{%>Chưa Cập Nhật<%}%>" name="address" >
                      <small id="emailHelpId" class="form-text text-muted">Help text</small>
                    </div>
                    <div class="form-group">
                      <label for="">Quyền Truy Cập</label>
                        <select class="form-control" name="role_id" id="">
                          <option value="<%= user.role_id._id%>"><%= user.role_id.name%></option>
                          <% role.forEach(item=>{ %>
                            <% if(item.name!=user.role_id.name){%>
                              <option value="<%= item._id%>"><%= item.name%></option>
                            <%}%>
                          <%})%>
                        </select>
                    </div>
                    <button type="submit" class="btn btn-outline-success btn-block">Cập Nhật </button>
                </form>
            </div>
          </div>
        </div>
        <div class="card-footer small text-muted">Updated yesterday at 11:59 PM</div>
      </div>
    </div>
    <!-- /.container-fluid-->
    <!-- /.content-wrapper-->
<% include footer%>